#include<stdio.h>
#include<string.h>
int main()
{
char a[i];
int i,k;
printf("\n enter the string:");
scanf("%s",&a[i]);
printf("\n enter the limit");
scanf("%d",&k);
for(i=0;i<k;i++)
{
printf("\n ",a[i]);
}
return0;
}
